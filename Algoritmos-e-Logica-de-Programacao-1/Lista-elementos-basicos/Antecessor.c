#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int ano, ant, suc;
    scanf("%d", &ano);
    ant=ano-1;
    suc=ano+1;
    printf("%d %d\n", ant, suc);
	return 0;
}